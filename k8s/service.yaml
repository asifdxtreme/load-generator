apiVersion: v1
kind: Service
metadata:
  name: load-generator
  labels:
    app: load-generator
spec:
  type: NodePort
  selector:
    app: load-generator
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
